server:
  port: 9000
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9100/eureka/
spring:
  application:
    name: dashboard

turbine:
  appConfig: user,order
  clusterNameExpression: new String("default")
hystrix:
  dashboard:
    proxy-stream-allow-list: localhost